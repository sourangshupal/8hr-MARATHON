# ==============================================================================
# PRODUCTION REQUIREMENTS — Cloud Run only (pinned versions from local venv)
# Eval/local-only packages (ragas, deepeval, streamlit) are excluded where possible.
# sentence-transformers + torch are kept for the mxbai local embedding fallback.
# Full local deps → requirements.txt
# ==============================================================================

# --- CORE API ---
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-dotenv==1.2.2
requests==2.33.1
numpy==2.4.4
pytz==2026.2

# --- GOOGLE CLOUD ---
google-cloud-storage==3.10.1
google-cloud-aiplatform==1.151.0
google-cloud-documentai==3.14.0
cloud-sql-python-connector==1.20.2
pg8000==1.31.5
sqlalchemy==2.0.49

# --- VECTOR DB & RETRIEVAL ---
qdrant-client==1.17.1
redis==7.4.0

# --- JINA API EMBEDDINGS + LOCAL FALLBACK ---
# sentence-transformers pulls torch; needed for the mxbai local fallback path.
sentence-transformers==3.2.1
torch==2.5.1

# --- LANGCHAIN & AGENTIC ORCHESTRATION ---
langchain==1.2.18
langgraph==1.1.10
langchain-google-vertexai==3.2.3
langchain-community==0.4.1
langchain-openai==1.2.1
pydantic==2.13.4

# --- GUARDRAILS ---
nemoguardrails==0.21.0
langchain-nvidia-ai-endpoints==1.3.0

# --- LLM GATEWAY ---
portkey-ai==2.3.0

# --- OBSERVABILITY ---
langsmith==0.8.3
logfire[fastapi,requests]==4.32.1
loguru==0.7.3
prometheus-client==0.25.0
prometheus-fastapi-instrumentator==8.0.2

# --- INGESTION PARSERS ---
python-pptx==1.0.2
python-docx==1.2.0
pypdf==6.11.0
beautifulsoup4==4.14.3
